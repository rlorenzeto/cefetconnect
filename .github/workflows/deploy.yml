name: Deploy CefetConnect

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Atualizar e Fazer o Build
        run: |
          cd ~/cefetconnect
          git pull
          docker compose up -d --build